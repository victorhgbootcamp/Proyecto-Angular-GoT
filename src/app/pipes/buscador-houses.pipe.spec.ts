import { BuscadorHousesPipe } from './buscador-houses.pipe';

describe('BuscadorHousesPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscadorHousesPipe();
    expect(pipe).toBeTruthy();
  });
});
